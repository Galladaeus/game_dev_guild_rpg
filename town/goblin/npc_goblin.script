function init(self)
end

function update(self, dt)
end

function on_message(self, message_id, message, sender)
	messagereceived = true
	local conversationTable = {3, "Hello", "I need your help", "Will you help me?"}
	if message_id == hash("chat") then
		msg.post("testGUI#gui", "guimessage", conversationTable)
		pprint(go.get_id())
	end
end
